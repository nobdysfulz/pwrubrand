Closing CTA band. Every page ends with one.

```jsx
<CTABanner title="Stop guessing. Start performing." body="Not sure which product fits where you are?" note="Just $49/month. Cancel Anytime." primaryLabel="Contact Us Now" />
```

One per page. `tone="gradient"` by default; use `tone="ink"` if the section above it is already gradient.
