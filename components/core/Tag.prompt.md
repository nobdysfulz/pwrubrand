Selectable filter chip, used for blog categories (AI, Branding, Mindset, …).

```jsx
<Tag active onClick={() => setCat('AI')}>AI</Tag>
```

Sentence case, not uppercase — that separates it from Badge.
