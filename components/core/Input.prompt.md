Text field with an uppercase micro-label — the brand's only input treatment.

```jsx
<Input label="Work email" placeholder="you@brokerage.com" hint="We never share it." />
```

Pass `textarea` for multi-line, `error` to switch the border and helper text to red.
