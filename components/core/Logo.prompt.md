The PWRU wordmark, rendered from the supplied PNG artwork.

```jsx
<Logo tone="white" height={34} assetBase="../.." />
```

Set `assetBase` to the relative path back to the design-system root so the image resolves. Use `tone="white"` on charcoal or gradient backgrounds. Never recolor, outline, or reconstruct the mark.
