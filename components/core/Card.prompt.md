The base surface for every boxed piece of content.

```jsx
<Card tone="light" interactive topAccent>…</Card>
```

Tones: light (default), subtle (grey page sections), ink (charcoal gradient), brand (magenta→violet). Never combine tone="brand" with a coloured child — keep brand cards to white type only.
